HU-2.3: Spring Boot setup with required dependencies for backend

- Installed Spring Boot DevTools for auto-reload during development.
- Configured PostgreSQL Driver for database connection.
- Added Spring Web for building RESTful APIs.
- Integrated Lombok to reduce boilerplate code in entities and services.
- Implemented Spring Data JPA with Hibernate for persistence layer.

QA Acceptance Criteria:
- Backend compiles successfully with all dependencies.
- pom.xml contains all configured libraries.
- Successful connection established with PostgreSQL.
- Spring Web and JPA ready for service development.
- Lombok works correctly across the project.

DoD:
- Configuration committed to branch HU-2.3-dev.
- Backend starts without errors in local environment.
- Dependencies validated by QA.
